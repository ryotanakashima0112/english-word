<p id="notice"><%= notice %></p>

<h1>英単語一覧ページ</h1>

<table>
  <thead>
    <tr>
      <th>Id</th>
      <th>Word</th>
      <th>Meaning</th>
      <th>Count</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @vocabularies.each do |vocabulary| %>
      <tr>
        <td><%= vocabulary.id %></td>
        <td><%= vocabulary.word %></td>
        <td><%= vocabulary.meaning %></td>
        <td><%= vocabulary.count %></td>
        <td><%= link_to 'Show', vocabulary %></td>
        <td><%= link_to 'Edit', edit_vocabulary_path(vocabulary) %></td>
        <td><%= link_to 'Destroy', vocabulary, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>
<%= paginate @vocabularies %>

<br>

<%= link_to '新しい単語を登録する', new_vocabulary_path %>
<%= link_to '英単語のテストを始める', test_path %>