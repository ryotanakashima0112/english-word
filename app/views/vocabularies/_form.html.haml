= form_with(model: vocabulary, local: true) do |form|
  - if vocabulary.errors.any?
    #error_explanation
      %h2
        = pluralize(vocabulary.errors.count, "error")
        prohibited this vocabulary from being saved:
      %ul
        - vocabulary.errors.full_messages.each do |message|
          %li= message
  .field
    = form.label "英単語"
    = form.text_field :word
  .field
    = form.label "意味"
    = form.text_field :meaning
  .field
    = form.label "間違えた回数"
    = form.text_field :count
  .actions
    = form.submit "新しい単語を登録する"
